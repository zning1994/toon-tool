{
  "name": "toonjs",
  "version": "0.1.0",
  "description": "TOON format CLI tool — convert, validate, diff",
  "type": "module",
  "bin": {
    "toon": "./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zning1994-agent/toon-tool.git"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
